@page
@model GetandTake.Pages.Categories.ImageModel

@if (Model.Category.ImagePath != null)
{
    string imagePath = Model.HostUrl + Model.Category.ImagePath;
    <img src="@imagePath" class="img-thumbnail" width="250px;" />
}